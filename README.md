# Express + PostgreSQL Simple Server

Uma API simples que usa Express + PostgreSQL para simular um cadastro de pessoas.

## Estrutura dos arquivos

- `index.js` - Estrutura base com as rotas do Express
- `repository.js` - Métodos que conectam no banco de dados e realiza as queries.